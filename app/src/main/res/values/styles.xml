<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BottomDialog" parent="@android:style/Theme.Dialog">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:backgroundDimEnabled">true</item><!--设置屏幕变暗-->
        <item name="android:windowBackground">@android:color/transparent</item><!--设置完全透明-->
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">false</item><!--是否半透明-->
        <item name="android:windowFrame">@null</item><!--设置无边框-->
        <item name="android:windowIsFloating">true</item><!--是否浮现在activity之上-->
        <item name="android:windowContentOverlay">@null</item><!--是否有遮盖,就是应用打开时开始的黑色背景，时间极短-->
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item><!--设置窗口软键盘的交互模式-->
        <item name="android:windowNoTitle">true</item><!--设置无标题-->
    </style>

    <style name="window_anim" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/window_enter</item>
        <item name="android:windowExitAnimation">@anim/window_out</item>
    </style>
</resources>
